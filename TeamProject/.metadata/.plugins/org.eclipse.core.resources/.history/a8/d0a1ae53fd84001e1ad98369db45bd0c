package foodmap;

import java.util.Vector;
import foodmap.Resturant;

public class Heart {
	Vector<String> HeartList = new Vector <String>();
	void Add(boolean like, String name) {
		if(like == true) {
			HeartList.add(name);
		}
	}
	void Delete(boolean like, String name) {
		if(like == false) {
			
		}
	}
	void Share() {
		
	}
}
